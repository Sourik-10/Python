import shutil

# Delete the entire directory along with all its files and subdirectories
shutil.rmtree("dir")

# Copy the contents of 'sol.txt' to a new file named 'john.txt'
# shutil.copy("sol.txt", "john.txt")

# Move the file 'john.txt' into the 'dir' directory
# shutil.move("john.txt", "dir/")
